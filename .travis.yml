language: java
sudo: required

before_install:
  - sudo apt-get purge oracle-java7-installer
  - sudo apt-get update
  - sudo apt-get install oracle-java8-installer

install:
  - mvn clean
  - mvn install